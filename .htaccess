RewriteEngine On

# Если файл или папка существуют — отдаём их напрямую
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Иначе перенаправляем всё на index.php
RewriteRule ^(.*)$ /index.php [QSA,L]